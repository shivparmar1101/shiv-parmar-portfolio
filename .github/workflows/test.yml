# Simple Test - Just to verify GitHub Actions works
name: Test Workflow

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Hello
        run: echo "Hello from GitHub Actions!"
      
      - name: Check Node
        run: node --version
